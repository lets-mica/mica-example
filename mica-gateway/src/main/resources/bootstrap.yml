server:
  port: 8081

spring:
  redis:
    host: localhost
    port: 6379
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr}
        file-extension: yaml
      discovery:
        server-addr: ${nacos.server-addr}

nacos:
  server-addr: nacos.didispace.com:80
