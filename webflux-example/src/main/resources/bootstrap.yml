server:
  port: 8084

spring:
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr}
        file-extension: yaml
      discovery:
        server-addr: ${nacos.server-addr}

nacos:
  server-addr: nacos.didispace.com:80
